---
interface Props {
	class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class="bg-bg-secondary rounded-lg border border-[rgba(255,255,255,0.1)] shadow-md hover:shadow-glow transition-shadow duration-medium {className}">
  <slot />
</div>
